import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vn.vku.fieldsurvey',
  appName: 'VKU Field Survey',
  webDir: 'dist',
  bundledWebRuntime: false,
  android: { backgroundColor: '#f5f8fc' }
};

export default config;
