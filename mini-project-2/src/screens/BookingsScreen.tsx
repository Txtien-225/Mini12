import { FlatList, Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
import { useBookingStore } from '../store/useBookingStore';
export function BookingsScreen() { const reservations = useBookingStore((s) => s.reservations); const cancel = useBookingStore((s) => s.cancel); return <SafeAreaView style={styles.safe}><FlatList contentContainerStyle={styles.content} data={reservations} keyExtractor={(item) => item.id} ListHeaderComponent={<><Text style={styles.kicker}>MY SPACE</Text><Text style={styles.title}>Your bookings</Text></>} ListEmptyComponent={<Text style={styles.empty}>No bookings yet. Pick a room and time slot to get started.</Text>} renderItem={({ item }) => <View style={styles.card}><Text style={styles.name}>{item.roomName}</Text><Text style={styles.meta}>{item.date} · {item.slot.label}</Text><Text style={styles.pass}>Pass: {item.pass}</Text><Pressable onPress={() => cancel(item.id)}><Text style={styles.cancel}>Cancel reservation</Text></Pressable></View>} /></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#f5f8fc' }, content: { padding: 20, gap: 14 }, kicker: { color: '#087fbd', fontSize: 11, letterSpacing: 2 }, title: { color: '#12395b', fontSize: 32, fontWeight: '800', marginBottom: 12 }, empty: { color: '#6b8298', backgroundColor: '#fff', padding: 18, borderRadius: 14 }, card: { backgroundColor: '#fff', padding: 18, borderRadius: 16, borderWidth: 1, borderColor: '#dbe5ed', gap: 7 }, name: { color: '#12395b', fontSize: 17, fontWeight: '800' }, meta: { color: '#6b8298' }, pass: { color: '#087fbd', fontSize: 11 }, cancel: { color: '#c62828', fontWeight: '700', marginTop: 6 } });
